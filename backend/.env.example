PORT=3001
# Em produção (ex.: Neon), use a linha abaixo e pode omitir DB_HOST, DB_USER, etc.
# DATABASE_URL=postgresql://usuario:senha@host/banco?sslmode=require
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
# Senha real do PostgreSQL no seu ambiente local
DB_PASSWORD=
DB_NAME=smartbudget
JWT_SECRET=troque_por_uma_chave_secreta_forte
